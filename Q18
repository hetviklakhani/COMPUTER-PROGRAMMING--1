#include <stdio.h>
int main()
{
float a,b,c,d;
printf("Enter the Length of the rectangle's side\n");
scanf("%f",&a);
printf("Enter the breadth of the rectangle's side\n");
scanf("%f",&b);
c=2*(a+b);
d=a*b;
printf("Perimeter of the rectangle is %f and Area is %f",c,d);
return 0;
}
