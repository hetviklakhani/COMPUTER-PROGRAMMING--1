#include<stdio.h>
#include<conio.h>
 int main()
 {
     int a,b,c,d;
 printf{"1:Add 2 numbers.\n 2: multiply 2 numbers. \n 3:odd or even. \n 4: divisible by 5 or not. \n 5: prime or not."};
 printf{"Enter your choice"};
 scanf{"%d",a};

 switch(a)
 Case 1;

     {
         int a,b,c;
     printf{"Ënter the numbers"};
     scanf{"%d , %d",&a , &b}
     c=a+b;
     printf{"%d + %d = %d\n", a,b,};
     }
     break;
     Case 2;
     {
             int a,b,c;
     printf{"Ënter the numbers"};
     scanf{"%d , %d",&a , &b}
     c=a*b;
     printf{"%d * %d = %d\n", a,b,};
     }
     break;
     Case 3;
     {
                 int a;
     printf{"Ënter an integer"};
     scanf{"%d" , &a};
     if (a% 2 == 0)
        printf("%d is even.\n",a);
     else
        printf("%d is odd.\n",a);


    }
    break;
    Case 4;
    {
         int a;
     printf{"Ënter an integer"};
     scanf{"%d" , &a};
     if (a% 5 == 0)
        printf("%d is divisible by 5.\n",a);
     else
        printf("%d is not divisible by 5.\n",a);

    }
    break;
    default : printf{"invalid choice"};
    }
